# =====================================================
# CONFIGURACIÓN BACKEND API
# =====================================================

# Entorno
NODE_ENV=production

# Puerto del servidor
PORT=8080

# Base de Datos MySQL (Google Cloud SQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=clinica_equilibrar_erp

# JWT
JWT_SECRET=cambia_este_secreto_en_produccion_usa_string_largo_y_aleatorio
JWT_EXPIRES_IN=24h

# Frontend URL (para CORS)
FRONTEND_URL=https://tu-dominio.com

# Opcional: Google Cloud SQL Connection Name
# INSTANCE_CONNECTION_NAME=proyecto:region:instancia
